#include <stdio.h> //standard input and output
//# directive
//# include preprocessor directive that tell compiler to put code
//<stdio.h> header file include all function
#include <limits.h>

//ex 2.1
int main() //every c program must have main function
{          //open bracket have to line up
    //signed char MIN MAX
    // printf("The min value of signed char = %d\n", SCHAR_MIN);
    // printf("The max value of signed char = %d\n", SCHAR_MAX);
    // printf("The maximum value of UNSIGNED CHAR = %d\n", UCHAR_MAX);

    // printf("The minimum value of SHORT INT = %d\n", SHRT_MIN);
    // printf("The maximum value of SHORT INT = %d\n", SHRT_MAX);
    // printf("The maximum value of UNSIGNED SHORT = %d\n", USHRT_MAX);

    // printf("The minimum value of INT = %d\n", INT_MIN);
    // printf("The maximum value of INT = %d\n", INT_MAX);

    // printf("The minimum value of CHAR = %d\n", CHAR_MIN);
    // printf("The maximum value of CHAR = %d\n", CHAR_MAX);

    // printf("The minimum value of LONG = %ld\n", LONG_MIN);
    // printf("The maximum value of LONG = %ld\n", LONG_MAX);
    // printf("Hello World!\n");

    // double c;
    // for (c = 0; getchar() != EOF; c++)
    // {
    //     printf("%.0f", c);
    // }

    // int c;
    // while ((c = getchar()) != EOF)
    // {
    //     // printf("%d ", c);
    //     putchar(c);
    // }

    return 0; //no error
}